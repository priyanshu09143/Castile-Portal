<%- include('../partials/head') %>
<%- include('../partials/nav') %>
<main class="main">
  <header class="page-header">
    <h1>Add purchase bill – <%= vendor.name %></h1>
    <a href="/vendors/<%= vendor._id %>" class="btn">Back to <%= vendor.name %></a>
  </header>

  <form action="/vendors/<%= vendor._id %>/purchase-bills" method="post" enctype="multipart/form-data" class="form form-card">
    <label>Amount (₹)</label>
    <input type="number" name="amount" min="0" step="0.01" required>

    <label>Note (optional)</label>
    <input type="text" name="note">

    <label>Bill image (optional)</label>
    <input type="file" name="image" accept="image/*">

    <label>Date</label>
    <input type="datetime-local" name="date" value="<%= new Date().toISOString().slice(0,16) %>">

    <button type="submit" class="btn btn-primary">Add purchase bill</button>
  </form>
</main>
<%- include('../partials/footer') %>
